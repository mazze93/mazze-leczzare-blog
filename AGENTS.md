# mazze-leczzare-blog — Codex Context

Personal blog. Built with Astro.

## Stack
- **Framework**: Astro (`astro.config.mjs`)
- **Functions**: `functions/` directory (likely Cloudflare/Netlify edge functions)
- **Output**: `dist/` (built static output)
- **Assets**: `brand-assets-showcase.html`, `_generate_showcase.py`

## Key Commands
```bash
npm run dev       # Dev server
npm run build     # Build to dist/
npm run preview   # Preview built output
```

## Notes
- `docs/` directory exists — check there for any authoring conventions
- `index.astro` at root is likely the landing page

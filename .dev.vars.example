CONTACT_FROM_EMAIL=contact@mazzeleczzare.com
CONTACT_SUBJECT_PREFIX=Mazze Contact
# Set these as Cloudflare secrets in real environments. Leave blank in this example file.
# Optional: POST validated contact payloads to a webhook or Worker endpoint.
CONTACT_WEBHOOK_URL=
# Optional: full Authorization header value expected by the webhook receiver.
CONTACT_WEBHOOK_AUTH_HEADER=

---
interface Props {
  src: string;
  title: string;
  height?: number;
  caption?: string;
  fullscreenHref?: string;
}

const {
  src,
  title,
  height = 620,
  caption,
  fullscreenHref = src,
} = Astro.props;
---

<figure class="artifact-embed">
  <div class="artifact-frame-wrap" style={`--frame-h: ${height}px`}>
    <iframe
      src={src}
      title={title}
      class="artifact-frame"
      loading="lazy"
      allowfullscreen
    ></iframe>
  </div>
  <figcaption class="artifact-caption">
    {caption && <span class="caption-text">{caption}</span>}
    <a href={fullscreenHref} class="fullscreen-link" target="_blank" rel="noopener noreferrer">
      Open full screen ↗
    </a>
  </figcaption>
</figure>

<style>
.artifact-embed {
  margin: 2.5em 0;
  border: 1px solid var(--rule);
  border-radius: var(--radius-md);
  overflow: hidden;
}

.artifact-frame-wrap {
  position: relative;
  width: 100%;
  height: var(--frame-h, 620px);
  overflow: hidden;
  background: #0F1923;
}

.artifact-frame {
  display: block;
  width: 100%;
  height: 100%;
  border: none;
  background: transparent;
}

.artifact-caption {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1em;
  padding: 0.65em 1em;
  border-top: 1px solid var(--rule);
  background: var(--surface, rgba(255,255,255,0.02));
}

.caption-text {
  font-family: var(--font-mono);
  font-size: 0.72em;
  color: var(--text-faint);
  line-height: 1.5;
  flex: 1;
}

.fullscreen-link {
  font-family: var(--font-mono);
  font-size: 0.68em;
  letter-spacing: 0.06em;
  color: var(--teal);
  text-decoration: none;
  white-space: nowrap;
  flex-shrink: 0;
  transition: color var(--transition-fast);
}

.fullscreen-link:hover {
  color: var(--coral);
}

@media (max-width: 800px) {
  /* Stacked, the artifact is taller than the desktop frame and needs to
     scroll internally. Grow the frame and let the iframe scroll; the
     "Open full screen" link remains the escape hatch for the full view. */
  .artifact-frame-wrap {
    height: min(82vh, 640px);
    min-height: 560px;
  }
}

@media (max-width: 640px) {
  .artifact-caption {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.4em;
  }
}
</style>
